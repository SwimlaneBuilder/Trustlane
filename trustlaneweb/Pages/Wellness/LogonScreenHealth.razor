@using Syncfusion.Blazor.Inputs

<h3>Logon Screen Health</h3>

The screen should imitate iOS launch guidelines and show an outline of the final app to improve the user experience.

Use the "campaign" or other feature to inspire better ideas.

Upload ideas into the catalog. Approval workflow, upvote, downvote, etc.

<div class="col-lg-12 control-section">
    <div class="control_wrapper">
        <SfUploader ID="UploadFiles" DropArea=".control-fluid" MaxFileSize=104857600>
            <UploaderAsyncSettings SaveUrl="https://aspnetmvc.syncfusion.com/services/api/uploadbox/Save"
                                   RemoveUrl="https://aspnetmvc.syncfusion.com/services/api/uploadbox/Remove"
                                   ChunkSize=500000></UploaderAsyncSettings>
        </SfUploader>
    </div>
</div>
<style>

    .control_wrapper {
        width: 350px;
        margin: 0 auto;
    }
</style>

@code {
    // pull inspiration (bible verses, etc) from groups
    // pull reminders for user in case of procrastication (reauthentcation)
    // pull random for AI stuff
}
